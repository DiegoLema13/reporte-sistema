services:
  - type: web
    name: reporte-sistema
    env: python
    buildCommand: |
      apt-get update
      apt-get install -y libreoffice
      pip install -r requirements.txt
    startCommand: gunicorn app:app